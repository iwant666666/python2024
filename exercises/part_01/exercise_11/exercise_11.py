def interleave(*lists):
    output = []
    for i in lists[1:]:

        m = output.append()

        n = list(zip( ))
        output.extend(n)
    return output


def main():
    interleave([1, 2, 3], [20, 30, 40], ['a', 'b', 'c'])
    # 倒推：得出【1，20，a】等 -> 用list(zip(n个【】))->如何得知所有的【】？